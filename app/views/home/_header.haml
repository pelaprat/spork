%br

- if @current_user
  .span-24{ :class => 'navigation last' }
    .span-12
      %a{ :id => 'spork', :href => '/' } Fieldnotes Database
      %br
      = link_to 'Add Fieldnote', new_fieldnote_path
      |
      = link_to 'Search', new_search_path

    .span-12{ :id => 'userinfo', :class => 'last'}
      - if session[:user_id]
        Logged in as:
        = link_to @current_user.fullname, user_path( @current_user )
        %br
        = link_to 'Users', users_path
        ||
        = link_to 'Logout', '/logout'
      - else
        = link_to 'Login', '/login'

%br
%br
%br
